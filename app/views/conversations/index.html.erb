<h1>Conversations:</h1>
<% @users.each do |user| %>
  <% if user.id != current_user.id && User.exists?(user.id)%>
  <%= link_to "Message: #{user.username}", conversations_path(sender_id: current_user.id, recipient_id: user.id), method: "post" %>
<br>

<% end %>
  <% end %>



